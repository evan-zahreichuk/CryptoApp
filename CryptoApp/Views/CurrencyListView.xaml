<Page x:Class="CryptoApp.Views.CurrencyListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Cryptocurrency Dashboard">
    <Page.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF1E3C72"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                    CornerRadius="5"
                    Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                    CornerRadius="5"
                    Padding="{TemplateBinding Padding}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    BorderBrush="{TemplateBinding BorderBrush}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Page.Background>
        <DynamicResource ResourceKey="PageBackgroundBrush"/>
    </Page.Background>
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Top Bar without theme toggle button -->
        <DockPanel Background="#FF1E3C72" LastChildFill="False" Height="50" Grid.Row="0">
            <TextBlock Text="Crypto Compass" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="10,0" DockPanel.Dock="Left"/>
        </DockPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Cryptocurrency Dashboard" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextForegroundBrush}" Margin="0,0,0,20" HorizontalAlignment="Center" Grid.Row="0"/>
                <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" Width="Auto">
                    <TextBox Width="300" Height="30" Margin="0,0,0,20" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    <Border Background="White" CornerRadius="10" Padding="10" MinWidth="300">
                        <ListView ItemsSource="{Binding FilteredCurrencies}" SelectedItem="{Binding SelectedCurrency}" SelectionChanged="ListView_SelectionChanged">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" Foreground="#FF1E3C72"/>
                                            <TextBlock Text="{Binding Symbol}" FontSize="12" Foreground="Gray"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding PriceUsd, StringFormat=C}" FontSize="14" VerticalAlignment="Center" Foreground="#FF2A5298"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                    <Button Content="Converter" Command="{Binding NavigateConverterCommand}" Width="150" Height="40" Margin="0,20,0,0"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Previous" Command="{Binding PreviousPageCommand}" Margin="5"/>
                    <TextBlock Text="{Binding CurrentPage}" FontSize="16" Foreground="{DynamicResource TextForegroundBrush}" VerticalAlignment="Center" Margin="5"/>
                    <Button Content="Next" Command="{Binding NextPageCommand}" Margin="5"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
